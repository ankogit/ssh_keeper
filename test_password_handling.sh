#!/bin/bash

# Тестовый скрипт для проверки работы с паролями в SSH Keeper
# Этот скрипт проверяет, что пароли правильно расшифровываются и передаются в экран редактирования

echo "🔍 Тестирование работы с паролями в SSH Keeper"
echo "=============================================="

# Проверяем, что приложение собралось
if [ ! -f "./ssh-keeper" ]; then
    echo "❌ Приложение не найдено. Сначала соберите его: go build -o ssh-keeper cmd/ssh-keeper/main.go"
    exit 1
fi

echo "✅ Приложение найдено"

# Проверяем конфигурационный файл
CONFIG_FILE="$HOME/.ssh/config"
if [ ! -f "$CONFIG_FILE" ]; then
    echo "⚠️  Конфигурационный файл SSH не найден: $CONFIG_FILE"
    echo "   Приложение создаст тестовые данные при первом запуске"
else
    echo "✅ Конфигурационный файл SSH найден: $CONFIG_FILE"
fi

echo ""
echo "📋 Инструкции для тестирования:"
echo "1. Запустите приложение: ./ssh-keeper"
echo "2. Если это первый запуск, установите мастер-пароль"
echo "3. Перейдите в 'Подключения' (клавиша 1)"
echo "4. Выберите подключение с паролем (иконка 🔒)"
echo "5. Нажмите Ctrl+E для редактирования"
echo "6. Проверьте, что поле 'Пароль' предзаполнено"
echo "7. Нажмите Ctrl+P для переключения видимости пароля"
echo "8. Убедитесь, что пароль отображается в открытом виде"
echo ""
echo "🔐 Ожидаемое поведение:"
echo "- Пароли должны загружаться в расшифрованном виде"
echo "- Поле пароля должно быть предзаполнено"
echo "- Пароль должен отображаться в открытом виде при нажатии Ctrl+P"
echo "- При сохранении пароль должен зашифровываться"
echo ""
echo "🚀 Запуск приложения..."
echo "   Нажмите Ctrl+C для выхода из теста"
echo ""

# Запускаем приложение
./ssh-keeper
